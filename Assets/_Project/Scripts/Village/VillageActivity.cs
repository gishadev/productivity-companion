namespace gishadev.companion.Village
{
    public enum VillageActivity
    {
        // Also covers a paused or stopped timer.
        Neutral = 0,

        // Running work session in a productive app.
        Working = 1,

        // Running break.
        Relaxing = 2
    }
}
